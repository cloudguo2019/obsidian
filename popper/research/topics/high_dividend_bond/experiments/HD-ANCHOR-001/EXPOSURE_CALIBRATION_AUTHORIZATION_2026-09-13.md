# HD-ANCHOR-001 阶段8暴露校准授权记录

> [已观察事实] 记录日期：2026-09-13（Asia/Shanghai）。

## 授权上下文

[已观察事实] 研究者在收到以下精确授权问题后回复“授权”：

> 是否授权我在 `[2019-03-11, 2022-03-10)` 开发/验证窗运行一次无绩效暴露校准，仅输出并冻结 `w_bar`、`q_raw` 和 `q_RM`？

## 本次允许范围

- [方法选择] 只运行一次P00无绩效暴露校准，窗口为左闭右开区间`[2019-03-11, 2022-03-10)`。
- [方法选择] P00全期基本面闸门固定输入`NORMAL`，执行冻结的PIT分红、两日确认、T日收盘信号至T+1日收盘成交、费用、税务、公司行动和逐日对账规则。
- [方法选择] 唯一允许固化的派生结果是`w_bar`、`q_raw`和`q_RM`；同时保留复现所需的输入哈希、运行计数及非绩效完整性状态。
- [方法选择] 若一次运行失败，不得无授权重跑；必须保存失败状态并重新取得研究者授权。

## 未授权范围

[方法选择] 本授权不包括正式回测、收益或风险指标、任何OOS结果、历史伪锁定开启、前向锁定开启、仿真或实盘。上述计数继续保持为0，`dry_run=true`。

